services:
  app:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - /app/node_modules
    ports:
      - "3001:3001"
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "npm run migration:run && npm run start:dev"
  db:
    image: mysql:8
    volumes:
      - ./mysql_db:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=my-nestjs-db
      - MYSQL_USER=tolik
      - MYSQL_PASSWORD=tolikPass
      - MYSQL_ROOT_PASSWORD=tolikSuperPass
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "tolik", "-ptolikSuperPass"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s